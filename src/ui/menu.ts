/**
 * Opacity of `@nstudio/nativescript-menu` popups on Android (0..1). The
 * plugin's glass default is translucent enough that items are hard to read
 * over chat text; iOS uses the system menu and ignores this.
 */
export const MENU_BACKGROUND_OPACITY = 0.9;
